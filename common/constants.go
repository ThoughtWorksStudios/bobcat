package common

const (
	INT_TYPE     = "$int"
	FLOAT_TYPE   = "$float"
	STRING_TYPE  = "$str"
	DATE_TYPE    = "$date"
	DICT_TYPE    = "$dict"
	BOOL_TYPE    = "$bool"
	ENUM_TYPE    = "$enum"
	SERIAL_TYPE  = "$incr"
	UID_TYPE     = "$uid"
	DIST_TYPE    = "$distribution"
	NORMAL_DIST  = "normal"
	UNIFORM_DIST = "uniform"
	WEIGHT_DIST  = "weight"
	PERCENT_DIST = "percent"
)
